steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/celestial-digit-466218-b3/livecricketscores:$SHORT_SHA', '.']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/celestial-digit-466218-b3/livecricketscores:$SHORT_SHA']

  -  name: "gcr.io/cloud-builders/gke-deploy"
     args:
     - run
     - --filename=app.yaml
     - --location=us-central1
     - --cluster=autopilot-cluster-1
images:
  - 'gcr.io/celestial-digit-466218-b3/livecricketscores:$SHORT_SHA'

options:
  logging: CLOUD_LOGGING_ONLY

